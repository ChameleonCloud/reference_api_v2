# reference api rewrite

## installation

```
python3 -m venv .venv
source .venv/bin/pip
pip install .
git submodule update --init 
```

## run for development

```
fastapi dev main.py
```

access swagger docs at `/docs` url
